# 系統分頁管理 — Design Brief + UX Spec

> **Version:** 2.0
> **Created:** 2026-05-28
> **Updated:** 2026-05-29（對齊 Prototype `html/系統分頁管理.html` 現況，Part 3 重寫）
> **用途：** 上傳至 Claude Design，配合文件末尾的 Prompt Cheatsheet 使用

---

## Part 1 — Design Brief

### Product Vision

系統分頁管理讓後台管理者能在統一介面中查看、設定與維護所有電商網站必要的系統頁面（如隱私權政策、退款政策、404 錯誤頁等）。這些頁面過去散落在各功能模組中，缺乏統一管理入口。透過本模組，管理者在網站上線前能快速確認每個必填頁面的完成狀態，並一站式完成所有頁面的內容設定與發布。

### Target User

**Primary：** 電商後台管理員（一般管理員 / 超級管理員），負責在網站上線前完成法律合規頁面與系統功能頁面的設定，需快速掌握哪些頁面尚未設定

**Secondary：** 超級管理員，需要修改頁面路徑（網址）或管理維護模式白名單

### Design Goals

| # | Goal | How to test |
|---|---|---|
| 1 | 管理者能在 30 秒內找到所有未設定的必填頁面 | 易用性測試：請用戶找出哪些必填頁面尚未設定，計時並觀察操作路徑 |
| 2 | 管理者能完整設定一個法律合規頁面並發布，全程不需離開系統分頁模組 | 觀察任務完成率與操作路徑，確認無跳轉其他模組 |
| 3 | 管理者在儲存後能立刻確認頁面狀態已更新為「已設定」 | 觀察用戶是否需要手動重新整理頁面才能確認結果 |

### Design Principles

1. **完成度優先** — 核心任務是確保所有必填頁面設定完成，介面應讓未完成項目視覺上突出，不隱沒於清單中
2. **編輯不干擾清單** — 點擊編輯進入獨立編輯頁，避免行內展開導致清單可見性下降
3. **自動儲存降低焦慮** — 編輯過程持續顯示自動儲存狀態，讓使用者不擔心草稿遺失

### Design System Reference

- **Figma Library:** Evomni Design System
- **Component framework:** 依 Evomni 設計系統規範
- **Icon set:** Evomni Design System 內建圖示集
- **Font:** 依 Evomni Design System 規範

### Accessibility Requirements

- **Target WCAG level:** AA
- **Known constraints:**
  - 狀態標籤不可單純以顏色區分，須搭配圖示或文字
  - 鍵盤可完整操作（Tab 導覽、Enter 確認、Esc 取消對話框）
  - 富文字編輯器需支援鍵盤操作與所見即所得格式套用

### Hard Constraints

- 系統分頁不可刪除（僅能停用），因為這些頁面由系統預定義
- 一般管理員不可修改頁面路徑（網址），路徑欄位對其顯示為唯讀

### Out of Scope

- 主選單頁面管理（由選單管理模組處理）
- 商品詳情頁、部落格文章頁
- 使用者自行建立的自訂頁面

---

## Part 2 — Screen Index

| # | 畫面名稱 | 導覽路徑 | 主要使用者目標 |
|---|---|---|---|
| 1 | 系統分頁列表頁 | 後台側邊選單 > 網站設計 > 系統分頁 | 確認哪些必填頁面尚未設定，快速進入編輯 |
| 2 | 頁面編輯頁（法律合規 / 一般內容） | 列表頁 > 點擊「編輯」（法律合規類頁面） | 填寫頁面完整內容、SEO 設定並發布 |
| 3 | 頁面編輯頁（功能性錯誤頁） | 列表頁 > 點擊「編輯」（404 / 500 / 維護中） | 自訂錯誤頁或維護頁的顯示文案與視覺元素並發布 |

---

## Part 3 — Per-Screen UX Spec

---

### Screen 1: 系統分頁列表頁

**Purpose:** 提供所有系統分頁的一覽表，讓管理者快速掌握設定完成率並導覽至需要編輯的頁面。
**Entry points:** 後台側邊選單 > 網站設計 > 系統分頁
**Primary user goal:** 找出尚未設定的必填頁面，點擊進入編輯

#### Information Hierarchy

```
H1 (most prominent): 系統分頁
Primary CTA: 無（此為管理列表，主要動作為各列的「編輯」）
Supporting info: 篩選列、頁面清單表格、各列操作連結
```

> 注意：原 Spec 規劃的設定完成度儀表板（進度條 + 必填頁面警告）目前 Prototype 未實作，已從本版本移除。如後續需補充，請重新評估規格後加回。

#### Actual Copy

**Page / Section Headings**
- Page title: `系統分頁`
- Breadcrumb: `網站設計 / 系統分頁`

**Filter / Search Bar**
- Status filter label: `狀態`
- Status options: `全部狀態` / `已設定` / `未設定` / `停用`
- Search placeholder: `搜尋頁面名稱...`

**Table Column Headers**
- `頁面名稱` / `狀態` / `路徑（網址）` / `操作`

**Status Badge Labels**
| 狀態鍵值 | 顯示文字 | 色彩語意 |
|---|---|---|
| set | 已設定 | 成功（綠色） |
| unset | 未設定 | 警告（橙色，帶警告圖示） |
| disabled | 停用 | 中性（灰色） |

**Action Buttons（每列）**
- Primary: `編輯`
- Secondary: `預覽`（狀態為「未設定」時灰色不可點擊）

**Empty State（搜尋 / 篩選無結果）**
- Headline: `找不到符合條件的頁面`
- Subtext: `請調整篩選條件或清除搜尋關鍵字`
- CTA label: `清除篩選`

**Error States**
| 錯誤條件 | 顯示訊息 |
|---|---|
| 網路錯誤，資料載入失敗 | `頁面清單載入失敗，請重新整理後再試` 附「重新整理」按鈕 |

**Loading State**
- Loading text: skeleton only（無文字，模擬表格列排版寬度）

**Toast / Notification Messages**
| 觸發 | 訊息 |
|---|---|
| 從編輯頁儲存成功後返回 | `頁面已儲存並發布` |

#### Component List

| 元件名稱 | 規格 / 變體 | 互動行為 |
|---|---|---|
| 篩選下拉選單 | 狀態篩選（4 選項） | 選擇後即時過濾列表，不需另外點擊確認 |
| 搜尋輸入框 | 帶清除（X）按鈕 | 輸入後即時過濾（無 debounce，直接 onChange 觸發） |
| 資料表格 | 固定表頭，4 欄（頁面名稱 / 狀態 / 路徑（網址）/ 操作）| 點擊「編輯」導覽至 Screen 2 或 Screen 3 |
| 狀態標籤（已設定） | 綠色圓點 + 文字，圓角 badge | 靜態顯示 |
| 狀態標籤（未設定） | 橙色警告三角圖示 + 文字，圓角 badge | 靜態顯示 |
| 狀態標籤（停用） | 灰色圓點 + 文字，圓角 badge | 靜態顯示 |
| 「編輯」連結 | 藍色文字連結（`color: #409EFF`），hover 底線 | 點擊導覽至對應編輯頁 |
| 「預覽」連結 | 眼睛圖示（EyeIcon）+ 文字「預覽」；未設定時顏色灰化（`cursor: not-allowed`）| 已設定時點擊顯示 Toast「{頁面名稱} 預覽已在新分頁開啟」；未設定時不可點擊 |
| 空狀態（篩選無結果） | 標題 + 說明文字 + 「清除篩選」按鈕 | 點擊「清除篩選」清空所有篩選 |
| Toast | 成功 / 資訊樣式，3 秒後自動消失 | 從編輯頁儲存成功返回後顯示 |

#### Screen States

| 狀態 | 觸發條件 | 呈現方式 |
|---|---|---|
| 預設（有資料） | 正常載入 | 顯示所有頁面列表 |
| 篩選後無結果 | 搜尋或篩選條件無符合項目 | 顯示空狀態（見上方 Empty State） |
| 網路錯誤 | API 請求失敗 | 錯誤訊息 + 重新整理按鈕 |

#### Interaction Annotations

| 使用者動作 | 結果 |
|---|---|
| 點擊「編輯」（一般內容頁） | 導覽至 Screen 2：法律合規編輯頁 |
| 點擊「編輯」（404 / 500 / 維護中） | 導覽至 Screen 3：功能性頁面編輯頁 |
| 點擊「預覽」（已設定頁面） | 顯示 Toast 提示，實際開啟新分頁為前台預覽（Prototype 模擬為 Toast） |
| 切換狀態篩選 | 即時過濾表格，不重新整理頁面 |
| 在搜尋框輸入文字 | 即時過濾頁面名稱（onChange 觸發，無 debounce） |
| 點擊「清除篩選」（空狀態） | 清空所有篩選條件，回到完整列表 |

---

### Screen 2: 頁面編輯頁（法律合規 / 一般內容）

**Purpose:** 讓管理者填寫與發布法律合規類系統頁面的完整內容，包含 SEO 設定。
**Entry points:** Screen 1 列表頁 > 點擊「編輯」（法律合規類頁面：隱私權政策、服務條款、退款政策等）
**Primary user goal:** 填寫頁面完整內容並點擊「儲存並發布」，使頁面在前台上線

#### Information Hierarchy

```
H1 (most prominent): 頁面名稱（如「隱私權政策」）
Tab navigation: 基本設定 / SEO（頁面頂端 Tab 切換）
Primary CTA: 儲存並發布
Secondary CTA: 儲存草稿
Supporting info:
  基本設定 Tab —
    左欄：路徑（網址）、狀態（非語系欄位），語系 Tab（頁面標題 + 頁面內容）
    右欄：頁面類型說明卡 + 前台預覽按鈕
  SEO Tab —
    語系選擇器（radio）、SEO 標題、SEO 描述（含字元計數）
```

#### 頁面整體 Layout

```
┌──────────────────────────────────────────────────────────────────────────┐
│  ← [H1 頁面名稱]  網站設計 / 系統分頁 / {頁面名稱}   [儲存草稿] [儲存並發布] │
├──────────────────────────────────────────────────────────────────────────┤
│  white container                                                         │
│  ┌──────────────────────────────────┬──────────────────────────────────┐ │
│  │  [基本設定] [SEO]（Tab bar）      │  （右欄 — 僅基本設定 Tab 顯示）  │ │
│  │  左欄 55%                        │  右欄 45%                        │ │
│  │                                  │  右欄預覽面板                    │ │
│  │  路徑（網址）                     │                                  │ │
│  │  狀態（啟用 / 停用 Radio）        │                                  │ │
│  │  ────────────────                │  ─────────────────               │ │
│  │  [繁中] [EN]（語系 Tab）          │  [前台預覽]（按鈕，置底）         │ │
│  │  > 頁面標題                      │                                  │ │
│  │  > 頁面內容（富文字編輯器）        │                                  │ │
│  └──────────────────────────────────┴──────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────┘
```

**SEO Tab（切換後左欄全寬，右欄隱藏）：**

```
  [繁中 radio] [EN radio]
  SEO 標題（input）
  SEO 描述（textarea，N / 160）
```

#### Actual Copy

**Page / Section Headings**
- Breadcrumb: `網站設計 / 系統分頁 / 隱私權政策`（以實際頁面名稱替換）
- Page title: `隱私權政策`（以實際頁面名稱替換）
- Tab labels: `基本設定` / `SEO`

**Button Labels**
- Primary action: `儲存並發布`（點擊後儲存並返回列表頁）
- Secondary action: `儲存草稿`（點擊後儲存草稿並返回列表頁）
- Back navigation: `←`（箭頭圖示，點擊返回，有未儲存變更時顯示確認對話框）
- Preview: `前台預覽`（位於右欄底部）

**Form Fields — 基本設定 Tab（非語系欄位，語系 Tab 外）**

| 欄位標籤 | 元件 | 說明 | 備註 |
|---|---|---|---|
| 路徑（網址） | 唯讀顯示框 或 [前綴] + Input + [編輯] 按鈕 | 功能性頁面（404/500/維護中）顯示「（系統觸發）」，唯讀；部分法律合規頁面唯讀 | 超級管理員對可編輯頁面顯示 Input，一般管理員一律唯讀 |
| 狀態 | Radio Group（啟用 / 停用） | 選擇後本地即時反映，儲存後生效 | 橫排，gap 20px |

**Form Fields — 基本設定 Tab（語系 Tab 內，可隨語系切換）**

| 欄位標籤 | 元件 | Placeholder | 說明 |
|---|---|---|---|
| 頁面標題 | Text Input（全寬） | 繁中：`顯示於瀏覽器標籤，建議 50–60 字元` / EN：`Page title for browser tab (max 60 chars)` | 必填 |
| 頁面內容 | 富文字編輯器（法律合規類） | `在此輸入頁面內容…` | 工具列：H2、H3、粗體、斜體、清單、圖片、表格 |

**Form Fields — SEO Tab**

| 欄位標籤 | 元件 | Placeholder | 說明 |
|---|---|---|---|
| （語系選擇） | Radio Group（繁中 / EN），圓角 badge 樣式 | — | 切換後下方欄位顯示對應語系內容 |
| SEO 標題 | Text Input（全寬） | 繁中：`輸入搜尋引擎顯示的頁面標題` / EN：`SEO title for search engines` | 建議 50–60 字元；留空將使用頁面標題 |
| SEO 描述 | Textarea（全寬，rows 4）右上角顯示 `N / 160` 字元計數 | 繁中：`輸入搜尋引擎顯示的頁面說明文字` / EN：`SEO description for search engines` | 建議 120–160 字元；超過 160 字元字元計數變紅，textarea 邊框變紅 |

**Multi-language Tabs（語系 Tab）**
- 繁體中文（`繁中`）— 預設選中，`background: #EFF6FF`，`color: #409EFF`，`border-bottom: 2px solid #409EFF`
- English（`EN`）— 非選中時白底，切換後顯示「English 版本尚未設定」提示區塊（虛線框，淺藍色）

**Error States**
| 錯誤條件 | 顯示訊息 |
|---|---|
| 儲存失敗（網路錯誤） | `儲存失敗，請確認網路連線後再試` |
| 路徑格式錯誤 | `路徑格式不正確，僅限英文、數字與連字號（-）` |
| SEO 描述超過 160 字元 | SEO 描述字元計數顯示紅色，textarea 邊框紅色 |

**Toast / Notification Messages**
| 觸發 | 訊息 | 類型 |
|---|---|---|
| 點擊「儲存並發布」成功 | `頁面已儲存並發布` | success |
| 點擊「儲存草稿」成功 | `草稿已儲存` | success |
| 儲存失敗 | `儲存失敗，請稍後再試` | error |

#### Component List

| 元件名稱 | 規格 / 變體 | 互動行為 |
|---|---|---|
| 返回按鈕（←） | 箭頭圖示，font-size 18px | 點擊時若有未儲存變更顯示確認對話框；否則直接返回列表頁 |
| 麵包屑 | 文字，最後一層不可點擊（灰色），前兩層可點擊返回 | 點擊上層返回對應頁面 |
| 「基本設定 / SEO」Tab bar | 下底線 active 樣式，`color: #409EFF` | 點擊切換 Tab；SEO Tab 時右欄隱藏 |
| 路徑（網址）— 可編輯 | `[https://domain.com]` 前綴 + Input + `[編輯]` 按鈕 | Input 聚焦時邊框變藍；失焦還原 |
| 路徑（網址）— 唯讀 | 唯讀樣式（`background: #F5F7FA`，`color: #909399`，`cursor: not-allowed`）| 無反應 |
| 狀態 Radio Group | 啟用 / 停用，`accentColor: #409EFF` | 選擇即時更新 form state |
| 語系 Tab（繁中 / EN）| SPLocaleTabs；繁中 active 底色 `#EFF6FF` | 切換語系；各語系內容獨立 |
| 富文字編輯器 | 工具列（H2、H3、粗體、斜體、清單、圖片、表格）+ Textarea | 工具列按鈕點擊高亮；Textarea 直接輸入 |
| 前台預覽按鈕 | 次要樣式（白底邊框），置於右欄底部 | 點擊顯示 Toast「前台預覽已在新分頁開啟」（info） |
| SEO 語系選擇 Radio | 圓角 badge 樣式，選中時 `background: #EFF6FF`，`border: 1px solid #409EFF` | 切換後 SEO 欄位顯示對應語系內容 |
| SEO 描述 Textarea | 帶字元計數 `N / 160`（右上角）；超過 160 字元計數變紅、邊框變紅 | 輸入即時更新字元計數 |
| 「儲存並發布」按鈕 | Primary 樣式（`background: #7C3AED`），有 Loading 狀態（`background: #909399`，`cursor: not-allowed`）| 點擊觸發儲存，成功後返回列表頁 |
| 「儲存草稿」按鈕 | Secondary 樣式（白底，邊框），有 Loading 狀態 | 點擊觸發草稿儲存，成功後返回列表頁 |
| 確認對話框（離頁警告） | 警告樣式，title「離開而不儲存？」，body「您有未儲存的變更，離開後將會遺失。確定要離開嗎？」| 「確定離開」（紅色）/ 「取消」 |
| Toast 通知 | 成功 / 錯誤樣式，3 秒自動消失 | 儲存完成或失敗後顯示 |

#### Screen States

| 狀態 | 觸發條件 | 呈現方式 |
|---|---|---|
| 新頁面（無內容） | 點擊尚未設定頁面的「編輯」 | 編輯器 placeholder；路徑欄位顯示預設值 |
| 已有內容 | 點擊已設定頁面的「編輯」 | 所有欄位填入既有資料 |
| 基本設定 Tab | 預設 | 左欄 55% + 右欄（類型說明卡）45% |
| SEO Tab | 點擊「SEO」Tab | 左欄全寬（100%），右欄隱藏 |
| 儲存中 | 點擊「儲存並發布」或「儲存草稿」| 按鈕顯示「儲存中…」，`background: #909399`，`cursor: not-allowed` |
| 儲存成功 | 儲存 API 回應成功 | Toast 顯示成功訊息，返回列表頁 |
| 儲存失敗 | 儲存 API 回應失敗 | Toast 顯示錯誤訊息，停留在編輯頁 |
| 路徑唯讀 | 功能性頁面 or 一般管理員 | 路徑顯示唯讀樣式（灰底，`cursor: not-allowed`） |

#### Interaction Annotations

| 使用者動作 | 結果 |
|---|---|
| 點擊「←」返回 | 若有未儲存變更，顯示確認對話框（確定離開 / 取消）；無變更則直接返回列表頁 |
| 點擊「基本設定」Tab | 顯示左右兩欄佈局（路徑 + 狀態 + 語系 Tab + 類型說明卡） |
| 點擊「SEO」Tab | 左欄全寬顯示 SEO 欄位（語系 radio + SEO標題 + SEO描述），右欄隱藏 |
| 切換語系 Tab（繁中 / EN）| 編輯器與頁面標題顯示對應語系內容；EN 尚未設定時顯示藍色虛線提示區塊 |
| 點擊「儲存並發布」 | 儲存中狀態 → API → 成功則 Toast 通知並返回列表頁 |
| 點擊「儲存草稿」 | 儲存中狀態 → API → 成功則 Toast 通知並返回列表頁 |
| 點擊「前台預覽」 | 顯示 Toast「前台預覽已在新分頁開啟」（info），模擬開啟前台 |
| 輸入 SEO 描述超過 160 字元 | 字元計數變紅，textarea 邊框變紅 |
| 點擊路徑唯讀欄位 | 無反應，`cursor: not-allowed` |
| 離開頁面時有未儲存變更 | 顯示確認對話框防止意外離頁 |

---

### Screen 3: 頁面編輯頁（功能性錯誤頁：404 / 500 / 維護中）

**Purpose:** 讓管理者自訂錯誤頁或維護頁的顯示文案、按鈕與視覺元素。
**Entry points:** Screen 1 列表頁 > 點擊「編輯」（404、500、維護中）
**Primary user goal:** 自訂錯誤 / 維護頁的標題、說明文字、按鈕與插圖，儲存並發布

> 本畫面繼承 Screen 2 的所有佈局與元件（Tab bar、左欄非語系欄位、語系 Tab、右欄頁面類型說明卡、儲存草稿 / 儲存並發布按鈕、離頁確認對話框），並以「錯誤頁自訂設定」結構化表單取代語系 Tab 內的富文字編輯器。以下僅記錄與 Screen 2 不同的部分。

> PRD 待補：PRD §4.3 缺失（文件從 §4.2 跳至 §4.4）。若有遺漏的功能規格，請補充至 PRD，本 UX Spec 以現有內容為準。

#### Information Hierarchy

```
H1 (most prominent): 頁面名稱（如「404 頁面」）
H2 (secondary): 錯誤頁自訂設定（結構化欄位區塊，取代富文字編輯器）
Primary CTA: 儲存並發布
Secondary CTA: 預覽
Supporting info: 右側設定面板（與 Screen 2 相同）
```

#### Actual Copy — 差異部分

**Additional Section Heading**
- `錯誤頁自訂設定`

**Form Fields — 錯誤頁自訂設定**
| 欄位標籤 | 404 提示文字 | 500 提示文字 | 維護中提示文字 | 說明文字 |
|---|---|---|---|---|
| 標題文字 | 找不到這個頁面 | 系統暫時無法服務 | 網站維護中 | 顯示於錯誤頁最顯眼位置 |
| 說明文字 | 您造訪的頁面不存在或已被移除 | 我們正在修復問題，請稍後再試 | 我們正在進行系統維護，敬請稍待 | 顯示於標題下方的補充說明 |
| 按鈕文字 | 回到首頁 | 重新整理 | （維護中無按鈕，此欄位隱藏） | 顯示於錯誤頁的行動按鈕文字 |
| 按鈕連結 | / | javascript:location.reload() | （維護中無按鈕，此欄位隱藏） | 按鈕點擊後的導向網址 |

**圖片上傳**
- 上傳區域說明: `拖曳或點擊上傳背景圖片或插圖`
- 格式提示: `支援 JPEG、PNG、SVG、WebP，建議寬度 800px 以上，上限 5MB`
- 已上傳狀態: 顯示縮圖 + 「刪除圖片」連結

**Error validation（差異部分）**
| 欄位 | 錯誤訊息 |
|---|---|
| 標題文字（空白） | `錯誤頁標題為必填欄位` |
| 按鈕連結（格式錯誤） | `請輸入有效的網址，須以 / 或 https:// 開頭` |
| 圖片上傳失敗 | `圖片上傳失敗，請確認檔案格式（JPEG / PNG / SVG / WebP）與大小（上限 5MB）` |

**維護中頁面額外區塊**（僅限維護中頁面顯示）
- Section heading: `維護模式設定`
- 欄位標籤: `預計恢復時間`
- 欄位說明: `填寫後前台將顯示倒數計時；留空則不顯示`
- 開關標籤: `啟用維護模式`
- 開關說明: `啟用後所有訪客（白名單 IP 除外）將看到此維護頁`
- 白名單提示: `如需設定可正常瀏覽的 IP 位址，請前往系統設定 > 維護模式管理`

**維護模式確認對話框**
- Title: `確定啟用維護模式？`
- Body: `啟用後，所有訪客（白名單 IP 除外）將立即看到維護頁，無法正常瀏覽網站。`
- Confirm button: `確定啟用`
- Cancel button: `取消`

**全域維護警示橫幅**（維護模式啟用時，後台所有頁面頂部）
- `目前網站處於維護模式，一般訪客無法瀏覽網站。`
- Link text: `前往維護模式設定`

#### Component List（差異部分）

| 元件名稱 | 規格 / 變體 | 互動行為 |
|---|---|---|
| 錯誤頁自訂設定區塊 | 結構化表單（取代富文字編輯器），欄位數依頁面類型而定 | 欄位輸入即時反映 |
| 圖片上傳元件 | 拖曳上傳區域 + 縮圖預覽；已上傳後顯示縮圖 + 檔名 + 「移除」連結 | 拖入或點擊選擇後即時反映；點擊「移除」直接清空（Prototype 無確認對話框） |
| 日期時間選擇器（維護頁限定） | `<input type="datetime-local">`，可選填 | 點擊開啟系統日曆選擇器 |
| 維護模式開關（維護頁限定） | 啟用 / 停用；啟用中顯示紅色強調背景（`#FEF0F0`）、紅色標籤文字 + 「啟用中」badge | 點擊從關閉→啟用時顯示確認對話框；點擊從啟用→關閉直接切換 |
| 確認對話框（維護模式） | 警告樣式，title「確定啟用維護模式？」，body「啟用後，所有訪客（白名單 IP 除外）將立即看到維護頁，無法正常瀏覽網站。」| 「確定啟用」（紅色）確認；「取消」關閉不執行 |
| 全域維護警示橫幅 | 固定後台頂部，`background: #FEF0F0`（淺紅），紅色文字 + 「前往維護模式設定」連結 | 點擊連結導覽至維護頁編輯頁 |

#### Screen States（差異部分）

| 狀態 | 觸發條件 | 呈現方式 |
|---|---|---|
| 維護模式啟用中 | 用戶啟用維護模式開關並確認 | 開關顯示紅色「啟用中」；全域警示橫幅出現於後台所有頁面 |
| 圖片上傳中 | 用戶選擇圖片後 | 上傳區域顯示進度條 |
| 圖片上傳完成 | 上傳 API 回應成功 | 顯示縮圖預覽與「刪除圖片」連結 |
| 圖片上傳失敗 | 上傳 API 回應失敗 | 上傳區域顯示錯誤訊息 |

#### Interaction Annotations（差異部分）

| 使用者動作 | 結果 |
|---|---|
| 點擊「啟用維護模式」開關 | 顯示確認對話框（確定啟用 / 取消） |
| 確認啟用維護模式 | 對話框關閉，開關切換為啟用（紅色），全域橫幅立即顯示於後台所有頁面 |
| 取消確認對話框 | 對話框關閉，開關保持關閉，不執行任何操作 |
| 上傳背景圖片 | 觸發上傳，顯示進度；完成後顯示縮圖 |
| 點擊「移除」（已上傳圖片） | 直接清空圖片欄位，無確認對話框（Prototype 現況） |
| 點擊「系統設定 > 維護模式管理」連結 | 導覽至系統設定的維護模式管理頁 |

---

## Part 4 — Cross-Screen Component Inventory

### Action Components

| 元件 | 變體 | 尺寸 | 狀態 |
|---|---|---|---|
| Button | Primary / Secondary / Ghost / Destructive | Medium / Small | Default / Hover / Active / Disabled / Loading |
| Icon Button | Ghost | Small | Default / Hover / Disabled |
| Toggle（開關） | 標準 / 警告（紅色，維護模式用） | — | Off / On / Disabled |
| Text Link | 標準 / 帶底線 | — | Default / Hover |

### Display Components

| 元件 | 變體 | 資料欄位 | 狀態 |
|---|---|---|---|
| Status Badge | 已設定（綠）/ 未設定（橙）/ 停用（灰） | 圖示 + 文字標籤，圓角 pill 樣式 | — |
| 全域維護警示橫幅 | 固定後台頂部，紅色背景（`#FEF0F0`）| 警示文字 + 導覽連結 | 啟用 / 隱藏 |
| Data Table Row | 標準列 | 頁面名稱 / 狀態 / 路徑（網址）/ 操作（4 欄）| Default / Hover（`#F5F7FA`）|
| 右欄預覽面板 | 灰底（`#F5F7FA`），含前台預覽按鈕 | 靜態顯示（僅基本設定 Tab 可見）|

### Input Components

| 元件 | 變體 | 狀態 |
|---|---|---|
| Text Input | 標準 / 唯讀 / 帶字元計數 | Default / Focus / Filled / Error / Disabled |
| Textarea | 帶字元計數（上限 160） | Default / Focus / Filled / Error |
| 富文字編輯器 | 標準工具列 | Default / Focus |
| Dropdown / Select | 單選 | Default / Open / Selected |
| 搜尋輸入框 | 帶清除（X）按鈕 | Default / Focus / Filled |
| File Upload | 拖曳上傳區域 + 縮圖預覽 | Default / Drag-over / Uploading / Complete / Error |
| Date-Time Picker | 可選填 | Default / Open / Selected |

### Navigation Components

| 元件 | 變體 | 說明 |
|---|---|---|
| 後台側邊選單 | 展開（含「網站設計 > 系統分頁」高亮） | 桌面後台全域導覽 |
| 麵包屑 | 文字，最後一層灰色不可點擊，前兩層可點擊返回 | 列表頁 2 層，編輯頁 3 層 |
| 返回按鈕（←）| 箭頭圖示，編輯頁頂部 | 搭配離頁確認對話框 |
| Tab bar（基本設定 / SEO）| 下底線 active 樣式 | 編輯頁頂部，切換編輯模式 |
| 語系 Tab（繁中 / EN）| 藍色 active 底色 | 語系內容切換，位於語系欄位包裝層 |

### Feedback Components

| 元件 | 變體 | 說明 |
|---|---|---|
| Toast | 成功 / 錯誤 / 資訊，3 秒後自動消失 | 右上角浮現（`top: 64px, right: 24px`）|
| Modal（確認對話框） | 警告樣式 | 用於維護模式啟用確認、離頁未儲存確認 |
| Empty State | 篩選無結果 | 標題 + 說明文字 + 「清除篩選」按鈕 |

---

## Part 5 — Design System Token Reference

| 設計決策 | 對應色彩語意 | 實際色碼（Prototype） |
|---|---|---|
| 已設定狀態標籤 | Success（綠色） | `#67C23A`，`background: #F0F9EB` |
| 未設定狀態標籤 | Warning（橙色） | `#E6A23C`，`background: #FEF0E6` |
| 停用狀態標籤 | Neutral（灰色） | `#909399`，`background: #F4F4F5` |
| 維護模式開關（啟用中） | Danger（紅色） | `#F56C6C`，背景 `#FEF0F0`，邊框 `#FBBFBF` |
| 全域維護警示橫幅 | Danger（紅色） | `background: #FEF0F0`，`border-bottom: #FBBFBF` |
| 主要按鈕（儲存並發布） | 紫色（Evomni 品牌色）| `#7C3AED`，hover `#6D28D9` |

---

## Assumptions

> 假設：列表頁狀態欄採「已設定 / 未設定 / 停用」三態設計，結合 PRD §4.1 的「啟用 / 停用」定義與 §6 模型圖的「已設定 / 未設定」顯示。三態可同時傳達內容完成度與發布狀態，PRD 兩處描述存在不一致，此為 UX 判斷，請 PM 確認最終定義。

> 假設：PRD §4.3 缺失（文件從 §4.2 跳至 §4.4）。若有遺漏的功能規格，請補充後更新本 UX Spec。

> 假設：維護模式的 IP 白名單設定不在系統分頁模組內，屬於系統設定模組，編輯頁僅以文字連結引導管理者前往設定。

> 假設：「預覽」功能僅限已設定頁面（有內容）。未設定頁面的「預覽」圖示按鈕灰色不可點擊（`cursor: not-allowed`）。

> 決定：草稿儲存改為手動觸發（「儲存草稿」按鈕），原 auto-save 機制已從 Spec 移除，以符合 Prototype 現況。如後續需補充 auto-save，請重新評估後更新本文件。

> Copy 待確認：500 錯誤頁「重新整理」按鈕連結目前 Prototype 為空字串（`''`），實際技術實作應為 `javascript:location.reload()` 或前端路由重新整理，請工程師確認後定案。

---

## Part 6 — Claude Design Prompt Cheatsheet

**使用說明：**
1. 將這份 DOCX 上傳到 Claude Design
2. 找到你想先做的畫面，複製下方對應的 prompt
3. 貼入 Claude Design 對話框，開始生成
4. 在 Claude Design 中直接對話迭代細節

---

### Screen 1 — 系統分頁列表頁

```
請幫我設計「系統分頁列表」的後台管理頁面 UI。這是一個桌面後台管理畫面，採用左側固定 Sidebar（展開「網站設計」並高亮「系統分頁」）加上主內容區的標準後台佈局。

頁面頂部有一個完成度儀表板，以橫向進度條加上文字「N / Y 頁面已設定」呈現，當有必填頁面未設定時顯示橙色警告提示；其下為搜尋篩選列（狀態下拉選單、搜尋框帶清除按鈕），再下方為頁面清單表格（欄位：頁面名稱 / 狀態 / 路徑（網址）/ 操作）。

狀態欄使用三種標籤：已設定（綠色圓點）、未設定（橙色警告圖示）、停用（灰色圓點）。每列右側有「編輯」文字連結與「預覽」圖示按鈕（未設定時灰色不可點擊）。

請参考上傳文件中 Screen 1 的完整規格，包含篩選無結果的空狀態以及 Skeleton 載入狀態。UI 語言為繁體中文。
```

---

### Screen 2 — 頁面編輯頁（法律合規 / 一般內容）

```
請幫我設計「系統分頁編輯（法律合規類）」的後台管理頁面 UI，以隱私權政策頁面為示範案例。這是一個桌面後台管理畫面，採用左側 Sidebar 加上主內容區的標準後台佈局。

主內容區分為左右兩欄：左側（約 60% 寬度）為所見即所得的富文字編輯器，含標準格式工具列（粗體、斜體、清單、連結、圖片、表格）與編輯區域；右側固定「頁面設定」面板（約 40% 寬度），由上到下包含：自動儲存狀態文字（「草稿已自動儲存 14:32」樣式）、頁面標題輸入框、路徑（網址）輸入框、啟用 / 停用開關、搜尋優化描述文字區（帶字元計數 N / 160）。

頁面頂部顯示麵包屑（網站設計 / 系統分頁 / 隱私權政策），右上角有「預覽」次要按鈕與「儲存並發布」主要按鈕。頁面頂部可選擇性顯示語系切換 Tab（繁體中文 / English）。

請参考上傳文件中 Screen 2 的完整規格，包含路徑唯讀狀態（一般管理員）與各欄位錯誤狀態。UI 語言為繁體中文。
```

---

### Screen 3 — 頁面編輯頁（功能性錯誤頁：404 / 500 / 維護中）

```
請幫我設計「系統分頁編輯（404 頁面）」的後台管理頁面 UI。佈局與法律合規頁面編輯器相同（左側 Sidebar 加主內容加右側設定面板），但主內容區改為「錯誤頁自訂設定」結構化表單，取代富文字編輯器。

主內容區「錯誤頁自訂設定」包含四個輸入欄位：標題文字（預填「找不到這個頁面」）、說明文字（預填「您造訪的頁面不存在或已被移除」）、按鈕文字（預填「回到首頁」）、按鈕連結（預填「/」），以及一個拖曳上傳元件用於上傳背景圖片或插圖（支援 SVG / PNG / WebP，顯示格式提示與尺寸建議）。

右側設定面板與法律合規編輯頁相同（頁面標題、路徑、狀態開關、搜尋優化描述）。另請設計維護中頁面的變體版本，底部額外顯示「維護模式設定」區塊，含預計恢復時間日期時間選擇器與紅色強調樣式的「啟用維護模式」開關，以及開啟開關後出現的確認對話框。

請參考上傳文件中 Screen 3 的完整規格，包含圖片上傳進度狀態、維護模式確認對話框、以及後台頂部的全域維護警示橫幅。UI 語言為繁體中文。
```

---

### 完整產品一次生成（可選）

```
請幫我設計「系統分頁管理」模組的完整 UI，共三個畫面。這是 Evomni 電商後台的子模組，統一管理網站必要的系統頁面（隱私權政策、服務條款、退款政策、404 錯誤頁、維護中頁面等），位於後台「網站設計 > 系統分頁」。

三個畫面分別為：
1. 系統分頁列表頁：含頁面頂部完成度儀表板（橫向進度條加文字），下方篩選搜尋列（狀態篩選 + 搜尋框），以及顯示全部系統頁面的資料表格（欄位：頁面名稱 / 狀態 / 路徑（網址）/ 操作；狀態標籤：已設定綠色 / 未設定橙色 / 停用灰色，每列含編輯與預覽操作）
2. 法律合規頁面編輯頁：左側富文字編輯器加右側設定面板（頁面標題、路徑、狀態開關、搜尋優化描述、自動儲存狀態），頂部有麵包屑與語系切換 Tab，右上角有儲存並發布主要按鈕
3. 功能性錯誤頁編輯頁：與第二個畫面佈局相同，但主內容改為結構化欄位（標題、說明、按鈕文字、按鈕連結、圖片上傳），維護中頁面另有維護模式開關（紅色強調）與確認對話框

所有畫面為桌面後台管理介面（左側 Sidebar 常駐展開），UI 語言繁體中文，請参考上傳文件中的完整 UX Spec 進行設計。
```
