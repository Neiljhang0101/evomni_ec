# 前台商品頁（列表 + 詳情）— 易用性測試報告 v2（修正後 Delta Report）

> **基準報告：** `SPEC/前台商品頁-易用性測試報告.md`（v1，2026-06-08）
> **修正日期：** 2026-06-08
> **修正檔案：** `html/商品列表.html`、`html/商品詳情.html`
> **修正範圍：** P0 × 1 項、P1 × 1 項、P2 × 3 項（共 5 項全部修正）

---

## 一、修正摘要

| 指標 | 修正前 | 修正後 | 變化 |
|---|---|---|---|
| 任務完成率 | 4 / 6（67%）| 6 / 6（100%）| +33% |
| Critical 問題 | 1 | 0 | -1 |
| Major 問題 | 1 | 0 | -1 |
| Minor 問題 | 3 | 0 | -3 |
| 遺留問題 | — | 0 | — |

**整體評語：** 本次修正解決所有 5 項問題。最大改善是商品詳情頁的商品說明/成分表 Tab 系統實作完成，消費者現在可直接在商品資訊下方切換查看商品說明與全成分表，不需再捲動到頁面後段尋找。商品列表頁完整修正響應式佈局，手機下顯示 2 欄 grid、類別 chips 及排序列，體驗與桌機版一致。本次無遺留問題。

---

## 二、問題追蹤表（Issue Tracker）

| # | 原始嚴重程度 | 問題描述 | 修正內容 | 驗證結果 |
|---|---|---|---|---|
| C-01 | 🔴 Critical | 商品詳情頁 `descTab` 宣告但 Tab 系統未實作 | 在加購推薦區塊下方、editorial 前插入新 Tab 系統：兩個 Tab 按鈕（商品說明 / 成分表）+ 條件渲染內容；商品說明頁含產品特色、主要功效（4 項 ul）、使用建議；成分表頁含全成分標示（Mono 字型）+ 不含成分說明；`descTab` state 正式接管 Tab 切換邏輯 | ✅ 已解決 |
| M-01 | 🟠 Major | 商品列表 grid inline style 阻擋手機 2 欄響應式 | 新增 CSS 規則（`<style>` 區塊）：`.listing-grid { grid-template-columns: repeat(3,1fr); gap: 24px }`；`@media (max-width: 900px)` 中將 grid 改為 2 欄、隱藏側欄、顯示 mobile-cat-chips 與 listing-filter-bar；移除 `listing-grid` div 的 inline `gridTemplateColumns` 與 `display: grid`；移除 `mobile-cat-chips` 及 `listing-filter-bar` 的 `display: none` inline | ✅ 已解決 |
| m-01 | 🟡 Minor | 立即購買 EvomniAlert 缺少確認按鈕文字 | 加入 `confirmText: '確認購買'` | ✅ 已解決 |
| m-02 | 🟡 Minor | 詳情頁 main padding-bottom 80px（Spec 100px）| 改為 `100px` | ✅ 已解決 |
| m-03 | 🟡 Minor | 收藏 icon 32×32px（Spec 28×28px）| 改為 `width: 28, height: 28` | ✅ 已解決 |

---

## 三、任務完成率對比

| 任務 | 修正前 | 修正後 | 關鍵修正 |
|---|---|---|---|
| T1 找到保養類商品進入詳情頁 | ✅ | ✅ | 無變化 |
| T2 選規格加入購物車含防呆 | ✅ | ✅ | 無變化 |
| T3 售完商品找到貨通知 | ✅ | ✅ | 無變化 |
| T4 查看商品說明和成分資訊 | ⚠️ 只能找到 editorial accordion | ✅ | Tab 系統實作，消費者可在商品資訊下方直接切換到「成分表」Tab |
| T5 搜尋功能找特定商品 | ✅ | ✅ | 無變化 |
| T6 手機底部 sticky CTA + 分類切換 | ⚠️ grid 3欄過小、chips 無法顯示 | ✅ | 響應式 grid 2欄；mobile-cat-chips 與 filter-bar 可正常顯示；padding-bottom 100px 防止內容被遮蓋 |

---

## 四、遺留問題清單

**本次無遺留問題。** 所有 P0、P1、P2 問題均已修正。

---

## 五、新發現問題（回歸 & 新增）

**回歸：無。**

**新發現（修正過程中觀察）：**

| # | 類型 | 嚴重程度 | 位置 | 問題描述 | 建議 |
|---|---|---|---|---|---|
| N-01 | 🆕 新發現 | 🔵 Enhancement | `html/商品詳情.html`，到貨通知 Toast | 到貨通知 Toast 說明文字為「補貨時將以 Email 通知您」，但消費者無法確認系統已記錄其 Email（因尚未登入或未輸入 Email）。若消費者為訪客，到貨通知可能沒有意義。 | 後端串接時，若消費者為未登入狀態，到貨通知動作應改為彈出登入提示或 Email 輸入欄。Prototype 可先維持現狀，加入 TODO 備注。 |
| N-02 | 🆕 新發現 | 🟡 Minor | `html/商品詳情.html`，`pdp-zoom-panel` CSS | Spec 要求 `.pdp-zoom-panel { display: none }` on mobile（防止觸控觸發放大窗），但當前 `<style>` 區塊無此規則。mousemove 在觸控裝置上通常不觸發，所以目前不構成可見問題，但正式開發應補此 CSS 規則。 | 加入 `@media (max-width: 900px) { .pdp-zoom-panel { display: none !important; } }` |

---

## 六、下一步行動

| 項目 | 負責人 | 目標完成日 |
|---|---|---|
| 確認到貨通知流程是否需要登入或 Email 輸入（N-01）| | |
| 加入 `.pdp-zoom-panel` 手機隱藏 CSS 規則（N-02）| | |
| 進行下一輪真人易用性測試（建議 3–5 位受測者）| | |
| API 串接後補入 Skeleton 骨架載入狀態 | | |
