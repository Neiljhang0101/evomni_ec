# 金物流串接設定 — 易用性測試報告 v2（修正驗證 Delta Report）

> **報告版本：** 2.0
> **執行日期：** 2026/05/14
> **測試模式：** SIMULATE 修正驗證（原始碼審查，對照 v1 報告逐項追蹤）
> **Prototype 路徑：** `html/金物流串接設定.html`
> **對應 v1 報告：** `SPEC/金物流串接設定-易用性測試報告.md`
> **修正範圍：** P0 全部（1 個）+ P1 全部（4 個）；P2 與 Enhancement 本次未修正

---

## 一、修正摘要

| 指標 | 修正前（v1）| 修正後（v2）| 變化 |
|---|---|---|---|
| 任務完成率 | 5/6 (83%) | 6/6 (100%) | +17% |
| Critical 問題 | 1 個 | 0 個 | -1 |
| Major 問題 | 4 個 | 0 個 | -4 |
| Minor 問題 | 6 個 | 6 個 | 0（本次未修） |
| Enhancement | 2 個 | 2 個 | 0（本次未修） |
| 遺留問題 | — | 8 個 | — |

**整體評語：** 本次修正解決了所有 P0 與 P1 問題，包含左側 Nav 的廠商啟用計數與異常紅點、廠商類型 Tag 的顏色辨識度、廠商資訊 Section 預設展開、Webhook 狀態 Badge 語義以及重量費率的空狀態引導。任務 T6（Webhook 失敗記錄查看）從部分完成升為完全完成。剩餘 6 個 Minor 問題及 2 個 Enhancement 留至下一輪修正。

---

## 二、問題追蹤表（Issue Tracker）

| # | 原始嚴重程度 | 問題描述 | 修正方式 | 驗證結果 |
|---|---|---|---|---|
| C-01 | Critical | 廠商類型 Tag 缺少類型識別符，無法視覺分辨廠商操作模式 | `VendorTypeTag` 以顏色區分四種類型：全自動=綠(#67C23A)、設定即用=藍(#409EFF)、半人工=橙(#E6A23C)、人工處理=灰(#909399)，取代 Spec 要求的 emoji（CLAUDE.md 禁止 emoji） | 已解決 |
| M-01 | Major | 左側 Nav 無任何啟用計數或異常紅點，Hub 頁面的全局狀態一覽失效 | `html/金物流串接設定.html` App 新增 `getCategoryMeta()` 從 `window.HUB_VENDORS` 計算各分類啟用數量與是否有異常；Nav 項目依此顯示數字計數或紅點，並以紅色文字標示異常分類 | 已解決 |
| M-02 | Major | VendorAutoScreen 廠商資訊 Section 預設收起，申請帳號連結對首次用戶不可見 | `VendorAutoScreen.jsx` 第 118 行：移除 `defaultOpen={false}` 參數，SectionCard 預設值為 `defaultOpen={true}` | 已解決 |
| M-03 | Major | WeightRulesPanel 啟用後直接顯示填好初始值的資料列，用戶會誤以為有既有設定 | `FreightSettingsScreen.jsx`：`tiers` 初始值改為 `[]`；`addTier` 處理空陣列時第一列 min 固定為 0；`deleteTier` 移除最後一筆保護；新增空狀態 UI（「尚未設定任何費率階梯」＋新增按鈕） | 已解決 |
| M-04 | Major | UtilScreens Webhook 清單的狀態 Badge 顯示「已啟用」而非「正常/異常」，語義與 Webhook 監控情境不符 | `SharedUI.jsx` StatusBadge 的 color map 新增 `'正常': '#67C23A'`；`UtilScreens.jsx` 第 200 行改為直接傳入 `w.status`（'正常' 或 '連線異常'） | 已解決 |
| m-01 | Minor | Hub 廠商 Card 底部數字（已啟用/總數）實際顯示內容與 Spec 要求格式不同 | 本次未修正（P2）| — 遺留 |
| m-02 | Minor | VendorAutoScreen 連線測試無節流顯示（「每分鐘最多 5 次」僅存於文案，無計時器或剩餘次數顯示）| 本次未修正（P2）| — 遺留 |
| m-03 | Minor | VendorOtherScreens 銀行金流設定頁的存摺影本上傳欄缺少 accepted file type 限制提示 | 本次未修正（P2）| — 遺留 |
| m-04 | Minor | VendorManualScreen 物流 Header 返回路徑文字為「串接設定」，但對應的 Nav 標籤文字為分類名稱（如「第三方物流」） | 本次未修正（P2）| — 遺留 |
| m-05 | Minor | TempRulesPanel 設定完成後 FixedBar 儲存訊息為「設定已儲存」，未說明影響範圍 | 本次未修正（P2）| — 遺留 |
| m-06 | Minor | InvoiceScreen 電子發票設定頁缺少「目前申請狀態」欄位，與 Spec §4.3 不符 | 本次未修正（P2）| — 遺留 |
| e-01 | Enhancement | Hub 廠商 Card 缺少「快速啟用/停用」開關，需進入設定頁才能切換狀態 | 本次未修正（Enhancement）| — 遺留 |
| e-02 | Enhancement | TempRulesPanel 費率表格無「批次刪除」功能，階梯數量多時管理不便 | 本次未修正（Enhancement）| — 遺留 |

---

## 三、任務完成率對比

| 任務 | 修正前（v1）| 修正後（v2）| 備註 |
|---|---|---|---|
| T1：找出金流連線異常廠商 | ✅ 完成（有摩擦）| ✅ 完成（順暢）| M-01 修正後左 Nav 紅點讓異常分類一目了然，不需進入每個分類查看 |
| T2：填寫綠界科技完整設定 | ✅ 完成（有摩擦）| ✅ 完成（順暢）| M-02 修正後廠商資訊 Section 預設展開，申請帳號引導連結立即可見 |
| T3：切換正式收款模式 | ✅ 完成 | ✅ 完成 | C-01 修正後全自動 Tag 呈綠色，無功能性影響；流程本身已正確 |
| T4：設定冷藏產品溫層運費 | ✅ 完成 | ✅ 完成 | 無改動，入口雖與 Spec 不同但功能可完成 |
| T5：聖喆國際追蹤號填入出貨確認 | ✅ 完成 | ✅ 完成 | 無改動，流程正確 |
| T6：查看 Webhook 失敗記錄 | ⚠️ 部分完成 | ✅ 完成 | M-04 修正後 Badge 正確顯示「正常」（綠）/ 「連線異常」（紅），與監控情境語義一致 |

---

## 四、遺留問題清單

| # | 嚴重程度 | 問題描述 | 遺留原因 | 建議下次處理 |
|---|---|---|---|---|
| m-01 | Minor | Hub 廠商 Card 啟用數字格式與 Spec 不完全吻合 | 視覺差異，不影響操作 | 下一輪視覺細修時一併調整 |
| m-02 | Minor | 連線測試無節流計數顯示 | 需新增 state 管理計時器，改動較大 | 實作前評估是否優先於其他功能 |
| m-03 | Minor | 銀行金流存摺影本上傳缺少 accept 提示 | 範圍有限，不阻礙主流程 | 下一輪補全上傳元件細節時處理 |
| m-04 | Minor | 物流設定頁返回路徑文字「串接設定」與 Nav 分類名稱不一致 | 文案差異，用戶可操作返回 | 統一 Breadcrumb 來源邏輯時修正 |
| m-05 | Minor | FixedBar 儲存訊息未說明影響範圍 | 文案優化 | 與 PM 確認 Toast 訊息規範後統一修改 |
| m-06 | Minor | InvoiceScreen 缺少申請狀態欄位 | Spec 要求但 Prototype 未實作，需補全頁面 | 下一輪 InvoiceScreen 補齊時實作 |
| e-01 | Enhancement | Hub 廠商 Card 缺少快速啟用開關 | 功能擴充，超出本次範圍 | 納入下版 HubScreen 設計評審 |
| e-02 | Enhancement | TempRulesPanel 費率表格無批次刪除 | 功能擴充，超出本次範圍 | 與 PM 確認使用場景後決定是否實作 |

---

## 五、新發現問題（回歸 & 新增）

本次修正無新增問題，無回歸。

所有修改均為局部且向後相容：
- `SharedUI.jsx`：VendorTypeTag 顏色替換（不破壞 API）；StatusBadge map 新增條目（不影響既有 status 值）
- `VendorAutoScreen.jsx`：移除 prop，SectionCard 回到預設行為
- `FreightSettingsScreen.jsx`：tiers 空陣列初始化，已正確處理 addTier 首列 min=0 的邊界條件
- `html/金物流串接設定.html`：getCategoryMeta 安全讀取 `window.HUB_VENDORS || {}`，HubScreen.jsx 未載入時不會報錯

---

## 六、下一步行動

| 項目 | 負責人 | 目標完成日 |
|---|---|---|
| 修正 InvoiceScreen 缺少申請狀態欄位（m-06） | | |
| 修正 Webhook 連線測試節流計數顯示（m-02） | | |
| 統一 Breadcrumb 返回路徑文字來源（m-04） | | |
| 評審 Hub 廠商 Card 快速啟用開關（e-01）是否納入下版 | | |
| 進行下一輪人工易用性測試（建議 3 位受測者，聚焦 T1 及 T4）| | |
