# 第三方登入 — 易用性測試報告 v2（Delta Report）

> **版本：** v2（修正後驗證）
> **基準報告：** `SPEC/第三方登入-易用性測試報告.md`（v1，2026-05-15）
> **修正日期：** 2026-05-15
> **Prototype 檔案：** `html/第三方登入.html`
> **修正範圍：** P0（I1、I2） + P1（I3、I4、I5、I6）共 6 個問題

---

## 一、修正摘要

| 指標 | 修正前 | 修正後 | 變化 |
|---|---|---|---|
| 任務完成率 | 5 / 7（71%） | 7 / 7（100%） | +29% |
| Critical 問題 | 2 個 | 0 個 | -2 |
| Major 問題 | 4 個 | 0 個 | -4 |
| Minor 問題 | 1 個 | 1 個（遺留） | — |
| 遺留問題 | — | 1 個（P2） | — |

整體評語：這次修正最大的改善是讓 Screen 5「填入憑證並儲存」任務真正可操作（由 ❌ 改為 ✅），排除了受控輸入與 state 脫節導致的錯誤回饋。另一個亮點是 Screen 4 的最後一種登入保護邏輯由靜態標記改為動態計算，解除任一帳號後保護邏輯立刻正確反映。遺留一個 P2 的 Loading Spinner 視覺優化問題，不影響功能性任務完成。

---

## 二、問題追蹤表

| # | 原始嚴重程度 | 問題描述 | 修正內容 | 修正位置 | 驗證結果 |
|---|---|---|---|---|---|
| I1 | 🔴 Critical | Screen 5 憑證欄位使用 `defaultValue`，儲存時讀取 state 舊值而非輸入框實際內容 | 新增 `formValues` state 追蹤各平台 clientId / secret；欄位改為受控組件（`value` + `onChange`）；`handleSave` 和 `handleTest` 改讀 `formValues[name]` | `html/第三方登入.html` L618–745 | ✅ 已解決 |
| I2 | 🔴 Critical | Screen 2 倒數計時器在切換 Screen 時未清除，回到頁面後秒數瞬間跳躍 | 加入 `React.useEffect` cleanup：`return () => { if (timerRef.current) clearInterval(timerRef.current); }` | `html/第三方登入.html` L305-307 | ✅ 已解決 |
| I3 | 🟠 Major | Screen 3「取消，不綁定」按鈕無 onClick，點擊後頁面無反應 | 加入 `onClick={() => showToast('已取消，OAuth 授權已清除，返回登入頁', 'info')}` | `html/第三方登入.html` L471 | ✅ 已解決 |
| I4 | 🟠 Major | Screen 1 切換 Tab 後 `showPw`（密碼顯示）及 `loginState`（錯誤狀態）不重置 | Tab onClick 改為 `() => { setTab(id); setShowPw(false); setLoginState('default'); }` | `html/第三方登入.html` L206 | ✅ 已解決 |
| I5 | 🟠 Major | Screen 4 的最後一種登入保護依賴靜態 `lastOnly` 標記，解除帳號後動態狀態不正確 | 移除 `lastOnly` 欄位；改為動態計算：`const linkedCount = Object.values(links).filter(l => l.linked).length; const isLast = info.linked && linkedCount === 1 && !hasPassword;` | `html/第三方登入.html` L487-489, L556-557 | ✅ 已解決 |
| I6 | 🟠 Major | Screen 5 拖曳排序說明寫「拖曳調整順序」但列表無實際拖曳功能，測試者可能誤以為 prototype 損壞 | 在說明文字後加 `（Prototype 展示版不支援拖曳操作）`，顏色 `#C0C4CC` 區隔 | `html/第三方登入.html` L791 | ✅ 已解決 |
| I7 | 🟡 Minor | Screen 1 社群按鈕 Loading 態時，logo 圖標消失，視覺偏空 | 本次未修正（P2） | — | — 遺留 |

---

## 三、任務完成率對比

| 任務 | 修正前 | 修正後 | 關鍵修正 |
|---|---|---|---|
| T1：以 LINE 登入 | ✅ 完成 | ✅ 完成 | I4：Tab 切換後狀態乾淨重置，消除登入 Tab 殘留的錯誤訊息 |
| T2：Email 補全 + 重新發送 | ✅ 完成 | ✅ 完成 | I2：切換 Screen 後倒數不再跳躍，使用者體驗連貫 |
| T3：帳號合併確認 + 取消 | ⚠️ 部分完成 | ✅ 完成 | I3：取消按鈕現在有 Toast 回饋，使用者確知操作成功 |
| T4：解除社群帳號連結 | ✅ 完成 | ✅ 完成 | I5：解除 LINE 後，Facebook 自動成為最後一種登入方式，「解除連結」按鈕正確 disabled |
| T5：填入 Google 憑證並儲存 | ❌ 無法完成 | ✅ 完成 | I1：輸入框現在受控，儲存讀取實際填入值，不再誤觸警告 Toast |
| T6：複製 Callback URL | ✅ 完成 | ✅ 完成 | 無需修正 |
| T7：切換僅社群登入模式 | ✅ 完成 | ✅ 完成 | 無需修正 |

---

## 四、遺留問題清單

| # | 嚴重程度 | 問題描述 | 遺留原因 | 建議下次處理 |
|---|---|---|---|---|
| I7 | 🟡 Minor | Screen 1 社群按鈕 Loading 態時 logo 圖標消失，視覺偏空，缺乏 loading spinner | P2 本次不列入修正範圍，不影響功能性任務完成 | 在 Loading 態加入旋轉 spinner SVG，預估工時 20 min |

---

## 五、新發現問題

本次修正無新增問題，無回歸。

---

## 六、Spec 合規再確認（修正後）

| 項目 | 狀態 |
|---|---|
| border-radius: 0 全域 | ✅ 修正未引入新的圓角 |
| fontWeight 只用 400/500/700 | ✅ |
| 色彩 Token 正確 | ✅ |
| 無 Emoji | ✅ |
| 受控輸入組件 | ✅ I1 修正後 Screen 5 憑證欄位全部改為受控 |

---

## 七、下一步行動

| 項目 | 建議負責人 | 目標完成日 |
|---|---|---|
| I7：Screen 1 Loading Spinner 視覺優化 | 設計 / 前端 | 下一個 iteration |
| Screen 4 / Screen 5 整併至現有 prototype（`html/會員管理後台.html`、`html/金物流串接設定.html`） | 前端 | 功能開發前 |
| 安排下一輪人工易用性測試（對應 Design Goals 的正式計時測試） | PM | 功能開發前 |
