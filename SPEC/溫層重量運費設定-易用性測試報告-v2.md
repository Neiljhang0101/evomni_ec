# Evomni 溫層/重量運費設定 — 易用性測試報告 v2（Delta 報告）

> **Version:** 2.0
> **Date:** 2026-05-15
> **Base report:** `SPEC/溫層重量運費設定-易用性測試報告.md`（v1）
> **Prototype:** `html/溫層重量運費設定.html`
> **修正方式:** AI 自動修正（prototype-iterate）

---

## 一、修正摘要

| 指標 | 修正前 | 修正後 | 變化 |
|---|---|---|---|
| 任務完成率 | 3/5（60%）| 5/5（100%）| +40% |
| Critical 問題 | 2 個 | 0 個 | -2 |
| Major 問題 | 2 個 | 0 個 | -2 |
| 遺留問題 | — | 2 個（P2）| — |

本次修正清除了所有 P0/P1 問題。最關鍵的改善是 S2/S3 儲存流程現在能正確阻擋不完整的設定並給出明確錯誤提示，商家不再會誤以為空白費率已成功儲存。S3 全域設定欄位在 Switch OFF 時的視覺 disabled 狀態也已補齊。

---

## 二、問題追蹤表（Issue Tracker）

| # | 原始嚴重程度 | 問題描述 | 修正方式 | 驗證結果 |
|---|---|---|---|---|
| 1 | P0 Critical | S2 `handleSave` 無驗證，Switch ON 時空白費率仍顯示儲存成功 | 在 `handleSave` 加入前置驗證：`coldRefEnabled` 時檢查 `islandBase`/`remoteBase` 非空；`coldFrzEnabled` 同理。驗證失敗顯示 error toast 並 `return` 阻止儲存。位置：`html/溫層重量運費設定.html:243` | ✅ 已解決 |
| 2 | P0 Critical | S3 `handleSave` 無驗證，預設重量空白、費率欄位空白、最後階梯上限非 0 時仍顯示成功 | 加入四層驗證：(1) `defWeight > 0`、(2) `tiers.length > 0`、(3) 每行 `islandFee`/`remoteFee` 非空、(4) 最後行 `max` 為 `'0'`/`0`/`''`（空值代表使用者尚未填入，允許儲存前提是其他驗證通過）。任一條件不符顯示對應 error toast。位置：`html/溫層重量運費設定.html:359` | ✅ 已解決 |
| 3 | P1 Major | S3 「重量單位」和「並存規則」RadioGroup 在 Switch OFF 時仍可點選 | 用 `<div style={{ opacity: enabled ? 1 : 0.55, pointerEvents: enabled ? 'auto' : 'none' }}>` 包住兩個 RadioGroup。位置：`html/溫層重量運費設定.html:390,407` | ✅ 已解決 |
| 4 | P1 Major | S4 Warning Banner 和運費佔位列使用 `borderRadius: 2`，違反 DS 規則 | 兩處 `borderRadius: 2` 改為 `borderRadius: 0`。位置：`html/溫層重量運費設定.html:605,625` | ✅ 已解決 |
| 5 | P2 Minor | S2/S3 取消 Dialog 未實際恢復上次儲存值 | 本次未修正（P2）| — 遺留 |
| 6 | P2 Minor | S3 Switch OFF 時費率階梯顯示文字提示而非灰化表格 | 本次未修正（P2）| — 遺留 |

---

## 三、任務完成率對比

| 任務 | 修正前 | 修正後 | 關鍵修正 |
|---|---|---|---|
| T1 商家設定產品溫層屬性（S1）| ✅ 完成 | ✅ 完成 | 無需修正 |
| T2 商家啟用冷藏運費並儲存（S2）| ⚠️ 部分完成 | ✅ 完成 | Fix #1：空白必填欄位現在阻擋儲存並顯示具體錯誤提示 |
| T3 商家啟用重量運費並建立費率階梯後儲存（S3）| ⚠️ 部分完成 | ✅ 完成 | Fix #2：四層驗證確保完整設定才能儲存；Fix #3：RadioGroup disabled 狀態清楚 |
| T4 消費者辨識混溫層警示（S4）| ✅ 完成 | ✅ 完成 | Fix #4：Banner 去除 borderRadius |
| T5 消費者看到運費金額與計算依據（S5）| ✅ 完成 | ✅ 完成 | 無需修正 |

---

## 四、遺留問題清單

| # | 嚴重程度 | 問題描述 | 遺留原因 | 建議下次處理 |
|---|---|---|---|---|
| 5 | P2 Minor | S2/S3 取消 Dialog 點「確定放棄」後未恢復上次儲存值 | 需加入 lastSaved snapshot 狀態管理，超出本次迭代範圍 | 下一輪 UI review 前補上，或在 Figma handoff 前確認 Cancel 行為是否需要在 prototype 中演示 |
| 6 | P2 Minor | S3 Switch OFF 時費率階梯顯示文字提示而非灰化 disabled 表格 | 需重構條件渲染結構，超出本次迭代範圍 | 可在 Figma 設計稿中補充 disabled 表格狀態截圖，不一定需要在 prototype 中完整呈現 |

---

## 五、新發現問題（回歸 & 新增）

本次修正無新增問題，無回歸。

---

## 六、下一步行動

| 項目 | 負責人 | 目標完成日 |
|---|---|---|
| P2#5 取消行為：確認 prototype 是否需演示 Cancel 恢復值（或接受文字說明） | PM / UX | — |
| P2#6 S3 Switch OFF 費率表：確認 Figma handoff 是否需補 disabled 表格截圖 | UX | — |
| 進行第二輪人工易用性測試（重點驗收 T2/T3 儲存驗證行為） | QA / UX | — |
