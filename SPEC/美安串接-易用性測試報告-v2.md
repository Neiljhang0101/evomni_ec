# 美安串接 — 易用性測試報告 v2（Delta Report）

> **版本：** 2.0
> **修正日期：** 2026/05/15
> **對照版本：** `SPEC/美安串接-易用性測試報告.md`（v1）
> **修正範圍：** 全部 7 個問題（P0 + P1 + P2 + Enhancement）
> **修正 Prototype：** `html/美安串接設定.html`

---

## 一、修正摘要

| 指標 | 修正前（v1）| 修正後（v2）| 變化 |
|---|---|---|---|
| 任務完成率 | 4/6（67%）| 6/6（100%）| +33% |
| Critical 問題 | 0 | 0 | — |
| Major 問題 | 3 | 0 | -3 |
| Minor 問題 | 3 | 0 | -3 |
| Enhancement | 1 | 0 | -1 |
| 遺留問題 | — | 0 | — |

**整體評語：** 本次修正後，所有 7 個問題均已解決。最大改善為 API Access Key 安全感知（由明文輸入改為密碼遮罩）以及報表匯出日期選擇體驗（加入清空按鈕、改為一致的區間選擇器）。Prototype 現在能完整展示初次設定空白流程，並支援最高權限 / 一般管理員角色切換，大幅提升後續易用性驗測的準確度。

---

## 二、問題追蹤表

| # | 原始嚴重程度 | 問題描述 | 修正方式 | 修正位置 | 驗證結果 |
|---|---|---|---|---|---|
| M1 | 🟠 Major | API Access Key 編輯模式使用明文輸入框，金鑰輸入時旁人可直接看見 | 將 `EInput` 替換為 `PasswordInput`（與 Secret Key 保持一致），金鑰以 `•••` 遮罩顯示 | 第 331 行 | ✅ 已解決 |
| M2 | 🟠 Major | 匯出 Dialog 使用兩個獨立 date input，無清空功能，使用者操作不直觀 | 新增 `DateRangeInput` 元件，整合「至」連接符與條件顯示的「清空」按鈕；以 `DateRangeInput` 取代兩個 `EInput` | 第 214–227 行（元件）、第 532–536 行（使用） | ✅ 已解決 |
| M3 | 🟠 Major | 手動傳送 Dialog 在輸入到一半時即顯示紅色錯誤訊息，違反 UX Spec | 移除 `FormField` 的 `error` prop，僅靠「確認傳送」按鈕 disabled 狀態傳達操作限制 | 第 779 行 | ✅ 已解決 |
| N1 | 🟡 Minor | Prototype 預設呈現「已儲存」狀態，無法驗測初次設定空欄位流程 | 新增 `demoMode` state 與 `switchToInitial()` / `switchToConfigured()` 函式；在 Screen 1 頂部加入 demo 狀態切換 banner | 第 234 行（state）、第 245–250 行（切換函式）、第 279–290 行（banner）| ✅ 已解決 |
| N2 | 🟡 Minor | 訂單來源篩選將標籤與預設值合併於 placeholder，選擇後標籤消失 | 在 `ESelect` 前加入獨立 `<span>訂單來源</span>` 文字標籤，placeholder 改為單純的「全部」 | 第 460 行（標籤）、第 463 行（placeholder）| ✅ 已解決 |
| N3 | 🟡 Minor | State B 傳送失敗的錯誤訊息以 inline 紅字呈現，視覺層級弱 | 新增 `ErrorBanner` 元件（紅底 `#FEF0F0`、左側 `IconX` 圖示），替換原 `<span>` 行內紅字 | 第 174–180 行（元件）、第 615 行（使用）| ✅ 已解決 |
| E1 | 🔵 Enhancement | `isSuperAdmin` 固定為 `true`，無法驗測一般管理員視圖 | 改為 `useState(true)`，在 Screen 3 demo banner 新增「最高權限 / 一般管理員」切換按鈕，即時控制 LOG 連結與手動傳送區塊的顯示 | 第 554 行（state）、第 683–692 行（切換 UI）| ✅ 已解決 |

---

## 三、任務完成率對比

| 任務 | 修正前 | 修正後 | 關鍵修正 |
|---|---|---|---|
| T1 — 填入 API 憑證並儲存 | ⚠️ 部分 | ✅ 完成 | M1（Access Key 密碼遮罩）+ N1（初次設定空白狀態可驗測）|
| T2 — 複製 XML Feed URL | ✅ 完成 | ✅ 完成 | 無變更，確認未回歸 |
| T3 — 識別美安訂單 | ✅ 完成 | ✅ 完成 | N2（篩選標籤更清晰）|
| T4 — 判讀傳送狀態 | ✅ 完成 | ✅ 完成 | N3（State B 錯誤訊息視覺更明確）|
| T5 — 傳送取消通知 | ✅ 完成 | ✅ 完成 | M3（Dialog 操作流程更乾淨，無多餘錯誤訊息干擾）|
| T6 — 匯出三個月美安報表 | ⚠️ 部分 | ✅ 完成 | M2（日期區間選擇器加入清空功能，操作直觀）|

---

## 四、遺留問題清單

本次修正已涵蓋全部 7 個問題，無遺留項目。

以下為原報告中標記為「Prototype 常見省略項目」的 ❌，屬設計驗收前預期差距，非 Prototype 設計問題，不列入修正範圍：

| 項目 | 說明 |
|---|---|
| 頁面載入中 Skeleton loader | 真實 API 環境才需要，Prototype 不呈現符合慣例 |
| 頁面載入失敗 Error state | 同上，Prototype 不呈現符合慣例 |
| 訂單列表 Loading Skeleton rows | 同上 |

---

## 五、新發現問題

本次修正無新增問題，無回歸。

修正過程中執行 DS 合規自動驗證：
- 所有新增按鈕均使用 `borderRadius: 0` — 合規
- 新增元件（ErrorBanner、DateRangeInput）無使用非法 border-radius — 合規
- 無 emoji 字元引入 — 合規
- 新增顏色值均在 Design System token 範圍內（`#FEF0F0` 為 error 背景、`#fde2e2` 為 error border）— 合規

---

## 六、修正細節記錄

```
✅ [M3] 🟠 Major — 移除 Screen 5 FormField 多餘 error prop
   位置: html/美安串接設定.html:779
   修正: 移除 error={confirmTxt && !isConfirmable ? '...' : ''} prop，
         使用者輸入到一半時不再顯示紅色錯誤訊息，僅靠按鈕 disabled 傳達限制

✅ [M1] 🟠 Major — API Access Key 改用 PasswordInput
   位置: html/美安串接設定.html:331
   修正: EInput 替換為 PasswordInput，Access Key 與 Secret Key 欄位行為一致，
         顯示/隱藏切換功能由 PasswordInput 元件內建提供

✅ [N3] 🟡 Minor — State B 錯誤訊息改用 ErrorBanner 元件
   位置: html/美安串接設定.html:174（元件定義）、615（使用）
   修正: 新增 ErrorBanner 函式元件（紅底背景 #FEF0F0、IconX 圖示），
         替換原本 inline <span style={{ color: '#F56C6C' }}>

✅ [N2] 🟡 Minor — 訂單來源篩選加入獨立文字標籤
   位置: html/美安串接設定.html:460
   修正: 加入 <span>訂單來源</span> 獨立標籤，placeholder 改為「全部」；
         ESelect width 調整為 130px

✅ [M2] 🟠 Major — 匯出 Dialog 日期選擇器改用 DateRangeInput
   位置: html/美安串接設定.html:214（元件定義）、532（使用）
   修正: 新增 DateRangeInput 元件，整合兩個日期輸入欄與條件性「清空」按鈕；
         有日期值時顯示清空，清空後恢復全部歷史記錄狀態

✅ [E1] 🔵 Enhancement — isSuperAdmin 改為可切換 useState
   位置: html/美安串接設定.html:554（state）、683（切換 UI）
   修正: const isSuperAdmin = true 改為 useState(true)；
         Screen 3 demo banner 加入「最高權限 / 一般管理員」角色切換按鈕

✅ [N1] 🟡 Minor — Screen 1 加入初次設定空白狀態切換
   位置: html/美安串接設定.html:234（demoMode state）、245（切換函式）、279（banner）
   修正: 新增 switchToInitial() 函式（清空所有欄位、取消遮罩）和
         switchToConfigured()（恢復預填值）；頂部 demo banner 可一鍵切換兩種狀態
```

---

## 七、下一步行動

| 項目 | 負責人 | 目標完成日 |
|---|---|---|
| 將 Prototype 修正同步至 Figma 設計稿（M1 密碼欄位樣式、M2 DateRangePicker、N3 ErrorBanner 樣式）| | |
| 安排 3–5 名非後台使用經驗的真人受測者進行易用性測試 | | |
| 確認 ErrorBanner 元件的 DS token 顏色（`#FEF0F0` / `#fde2e2`）是否已納入 Evomni Design System | | |
| 工程師確認 Screen 6 DateRangePicker 對應的 el-date-picker API 及清空行為 | | |
