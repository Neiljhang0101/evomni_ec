# Prototype 執行清單

> 追蹤來源：`PM提交_Prototype規劃清單_20260615.md`
> 每項目兩個勾選：**實作完成** + **PRD 驗測通過**，兩格都打勾才算結案
> 格式：`- [ ] 實作　[ ] 驗測`

---

## 後台（管理員端）

### 待修改

#### 訂單管理（`電商系統 > 訂單管理`）｜Part3 §6.1

- [x] 實作　[x] 驗測　**#1-高** 新增「已退款」「已關閉」Tab 及對應篩選選項
- [x] 實作　[x] 驗測　**#2-高** 訂單狀態 Tag 擴為 10 種；已出貨/配送中改綠色，已退款/已關閉加灰色
- [x] 實作　[x] 驗測　**#3-高** 已退款/已關閉狀態 Tooltip 補齊三層文案（What / Impact / Next Step），全 10 種狀態均補齊
- [x] 實作　[x] 驗測　**#4-高** 混溫層訂單標示：列表頁顯示「分批出貨」橙色 Badge，詳情頁顯示關聯子訂單提示

#### 行銷活動（`電商系統 > 行銷中心 > 優惠活動`）｜Part4 §6.2

- [x] 實作　[x] 驗測　**#5-中** 免運活動補齊免運類型（整單/部分商品）、條件類型、條件數值、計算基準欄位（§6.2C）
- [x] 實作　[x] 驗測　**#6-中** 折扣活動補齊多階梯設定（1–5 組、折扣條件類型、折扣類型、即時顯示最高可觸達階梯）（§6.2C）
- [x] 實作　[x] 驗測　**#7-中** 批次操作補齊跨頁選取（Checkbox 欄、兩段式選取 Banner、500 筆上限提示、批次設定 Modal 含共用上限/指定值模式、操作後自動清除）— 實作於 `UnifiedDiscountListPage`（§6.2F.1.D）

#### 金物流串接設定（`電商設定 > 金物流串接`）｜串接設定後台管理

- [x] 實作　[x] 驗測　**#8-中** 狀態 Badge 從「連線異常」拆為「連線測試失敗」與「串接異常」，附判定邏輯說明（§6.1）— 依 V3 v1.4 方案 c：兩來源（連線測試／Webhook）任一失敗即顯紅 Badge，僅連線測試失敗（Webhook 正常）顯示「連線測試失敗」，Webhook 連續失敗（門檻 3）或兩者皆失敗顯示「串接異常」，兩者皆正常才撤銷；Hub 頁新增「狀態說明」Legend 闡述此判定；ecpay 示例＝串接異常、eclogistics 示例＝連線測試失敗；廠商詳情頁於 串接異常 時在測試按鈕上方顯示持久 Alert（§6.2）
- [x] 實作　[x] 驗測　**#9-中** Webhook URL 格式補齊 uuid 段（`/api/webhooks/payment/[uuid]/[廠商代號]`）（§6.2B）— 全自動廠商頁與 Webhook 管理頁均補齊 `a3f9k2` 商店識別碼段並加註避免網址枚舉的說明
- [x] 實作　[x] 驗測　**#10-中** 批次匯入模式選擇 Dialog（合併 / 全部替換，後者附紅色警示確認）（§6.5B）— 半人工廠商頁 SKU 對照表新增「新增對照／批次匯入／下載對照表」操作列，批次匯入彈出模式選擇 Dialog，選「全部替換」時顯示紅色警示且確認鈕轉為紅色危險樣式

#### 會員管理後台（`顧客 > 顧客管理`）｜Part6

- [x] 實作　[x] 驗測　**#11-中** 黑名單：加入後標示「立即撤銷登入」狀態，補齊既有訂單依狀態分流處置表（§6.7C）— 詳情頁黑名單 Banner 顯示「已立即撤銷登入」狀態與處置表（details 展開）；加入黑名單 Dialog 內列出立即生效項目＋4 列訂單狀態分流處置表；toast 補「強制登出所有裝置」。同步更新 `html/會員管理後台.html` 與整合版 `components/pages/PageMembers.jsx`
- [x] 實作　[x] 驗測　**#12-中** 系統自動標籤：標示商家不可刪除/改名保護提示（§6.5A）— 標籤管理「系統自動標籤」分頁 InfoBanner 補保護說明＋新增「保護狀態」欄（系統保護不可刪除／改名）；詳情頁標籤卡標題補註。同步更新 html 與 `PageMembers.jsx`

#### 行銷追蹤碼管理（`電商設定 > 行銷追蹤碼`）｜行銷追蹤碼管理 §3

- [x] 實作　[x] 驗測　**#13-低** 入口位置從全域設定改為行銷管理（麵包屑路徑更新）— Sidebar 將「行銷追蹤碼」移至「行銷中心」群組並更新 section 對應；頁面新增「行銷中心 / 行銷追蹤碼管理」麵包屑。同步更新 `html/行銷追蹤碼管理.html` 與 `components/pages/PageTracking.jsx`

#### 方案管理（`全域設定 > 方案狀態`）｜方案管理 §6.1.B

- [x] 實作　[x] 驗測　**#14-低** 啟航方案商品上限標示（1,000 件）；諮詢 CTA 連結文字改為「預約方案諮詢」— 功能清單「產品管理（無上限）」改為「產品管理（上限 1,000 件）」；底部 CTA 文字改「預約方案諮詢」並連結 https://www.webtech.com.tw/contact（新分頁）。同步更新 `html/方案管理與升級流程.html` 與 `components/pages/PagePlan.jsx`

---

### 待建立

- [x] 實作　[x] 驗測　**#15-低** 通知信範本管理（通知信範本列表、範本預覽與編輯）｜`Evomni_通知信範本規格.md`— 新建 `html/通知信範本管理.html` 與共用元件 `components/pages/PageNotificationTemplates.jsx`（由 PRD 規格解析全 35 種範本：訂單×14／會員×7／方案×8／一頁式×5／邀請×1）；含分類篩選、搜尋、啟用切換、預覽 Dialog、編輯 Drawer（主旨／內文＋變數插入＋即時預覽＋還原預設）。已整合進 `index.html` 路由與 Sidebar（電商設定 > 通知信範本）。範本內容依最高原則移除 Emoji

---

## 前台（消費者端）

### 待修改

- [x] 實作　[x] 驗測　**#16-高** `訂單查詢（訪客）` — 第二驗證欄位：手機末四碼 → Email（input type=email、格式驗證、helper text 全部更新）｜Part3 §6.5 — 欄位改 Email（type=email、Email 格式驗證、失敗提示「請輸入有效的電子信箱格式」）；說明文字、錯誤態、查無資料態全數改為 Email 描述；驗證邏輯由 4 碼數字改為 Email regex
- [x] 實作　[x] 驗測　**#17-中** `結帳` — 0 元訂單跳過金流的 UI 狀態說明（付款方式區隱藏/顯示「免費商品，無需付款」）｜購物車與結帳流程 §5.6 — 新增「免費商品（0 元）」示意狀態：付款方式區隱藏選項、改顯示「免費商品，無需付款」說明（跳過金流、直接成立並標示已付款）；訂單摘要加「免費商品折抵」列、結帳金額 NT$ 0、送出鈕改「確認訂單（免付款）」
- [x] 實作　[x] 驗測　**#18-中** `訂單確認` — 訂單狀態 10 種（含已退款/已關閉）的 Tag 展示邏輯｜Part3 §2.2 — 新增可重用 `OrderStatusTag`（10 種狀態的顏色對應 + ⓘ Tooltip 說明）；確認頁訂單編號旁顯示狀態 Tag，並提供 10 種狀態預覽切換條
- [x] 實作　[x] 驗測　**#19-低** `會員中心` — 確認「記住我」登入選項在前台登入流程中是否呈現（7 天/30 天說明文字）｜前台會員登入 §5.3 — 會員中心「登入裝置管理」新增工作階段有效期說明（未勾選 7 天／勾選記住我 30 天，到期不自動延長），每筆裝置標示工作階段類型與到期日。註：登入頁「記住我」checkbox 將於 #20 登入頁實作

---

### 待建立

#### 會員認證流程（前台會員登入 PRD v1.7）

- [x] 實作　[x] 驗測　**#20-高** `登入` — Email+密碼表單、第三方快速登入（LINE/Google/FB）、錯誤態 × 5、CAPTCHA 觸發 — 建於 `html/會員登入.html`（單檔含 5 頁＋狀態切換條）；Email/密碼（眼睛切換）、記住我（7／30 天）、忘記密碼／立即註冊；6 種錯誤態（帳密錯誤、未驗證 Email、未驗證人工審核、IP 速率限制、暫鎖 15 分、系統自動停用、管理員停用）＋ CAPTCHA「我不是機器人」；LINE／Google 社群按鈕（FB v1.0 不顯示，依 PRD §6.1E）。程式驗測 24/24
- [x] 實作　[x] 驗測　**#21-高** `忘記密碼` — Email 輸入框、60 秒倒數重新發送、同頁切換成功態 — 表單→成功態同頁切換；成功態顯示防列舉文案＋「重新發送」60 秒冷卻倒數＋返回登入
- [x] 實作　[x] 驗測　**#22-高** `重設密碼` — 新密碼輸入、強度指示條、確認新密碼、Token 過期態與更新成功態 — 即時強度條（弱/普通/強/非常強）＋規則勾選（8 字元、英＋數）、確認不一致提示、Token 失效態、更新成功態；決議 22-1：重設成功但帳號仍停用以 EvomniAlert 橘色警示
- [x] 實作　[x] 驗測　**#23-中** `帳號驗證` — Token 有效啟用成功頁；Token 無效/已使用失效提示 + 重發按鈕 — 啟用成功頁（前往登入）／連結失效頁（重發驗證信）
- [x] 實作　[x] 驗測　**#24-中** `重發驗證信` — Email 輸入框、60 秒倒數、返回登入連結 — 表單（Email 可帶入修改）→ 已發送態，60 秒冷卻倒數＋返回登入

#### 第三方登入前台流程（第三方登入 PRD v1.1）

- [x] 實作　[x] 驗測　**#25-高** `OAuth 授權中 Loading 頁` — 「正在連接 {平台名稱}...」Loading 態 — 併入 `html/會員登入.html`（新增 OAuth／Email 補全／帳號合併 3 頁）；spin 動畫＋平台色，登入頁 LINE／Google 社群鈕直接觸發此頁，含後續流程示意連結。程式驗測 19/19
- [x] 實作　[x] 驗測　**#26-高** `Email 補全頁` — 已連接平台名稱（唯讀）、Email 輸入框、60 秒倒數等待態 — 平台徽章（唯讀）＋顯示名稱問候；Email 即時格式驗證（`/.+@.+\..+/`）→ 發送驗證信 → 驗證信等待頁（已發送至 {email}＋60 秒重發倒數）；另含驗證連結逾時態（重新登入）；隱私說明
- [x] 實作　[x] 驗測　**#27-高** `帳號合併確認頁` — 現有帳號提示、遮蔽 Email 顯示、密碼驗證、確認綁定/取消/不記得密碼連結 — 找到現有帳號＋遮蔽 Email（wa***@gmail.com）；密碼驗證框、確認綁定／取消不綁定、不記得密碼？發送密碼重設信；含密碼錯誤態（還有 N 次）與 5 次鎖定 30 分（確認綁定 disabled）

#### 一頁式商店前台（一頁式商店 PRD v2.7，進階電商包）

- [x] 實作　[x] 驗測　**#28-中** `一頁式商店 > 銷售頁` — Hero 區、倒數計時、賣點卡片、評論區、固定底部 CTA Bar（沉浸式，無導覽選單）— 建於 `html/一頁式商店.html`（手機優先、無 Sidebar/Header）：Hero 主視覺＋立即購買 CTA、`#303133` 倒數計時 HH:MM:SS（tabular-nums）、4 張賣點卡片、評論區（星等＋查看更多）、固定底部 CTA Bar（縮圖＋名稱＋售價＋立即購買）。程式驗測 19/19
- [x] 實作　[x] 驗測　**#29-中** 規格加購 Bottom Sheet（嵌入式） — 同商品詳情頁規格選擇流程，含低溫宅配選項自動顯示 — 由底部滑入 Bottom Sheet：規格 Tag（無庫存灰底斜線 disabled、未選按結帳整區抖動＋提示）、數量加減（剩餘 N 件）、加購推薦橫向卡片、購物車摘要、前往結帳；低溫商品自動顯示低溫宅配提示
- [x] 實作　[x] 驗測　**#30-中** `一頁式商店 > 結帳` — 同標準結帳頁流程，無導覽選單，維持沉浸式體驗 — 沉浸式結帳（Logo 置中、進度條 3 步驟、無離開連結）：收件表單即時驗證（欄位紅框非彈窗）、低溫商品自動套用低溫宅配（過濾其他物流）、金流、優惠碼套用、明細、確認下單並付款 → 付款成功頁（訂單編號／確認信／LINE 分享）

---

## 進度總覽

| 類別 | 項目數 | 實作完成 | 驗測通過 |
|---|---|---|---|
| 後台 待修改 | 14 | 14 | 14 |
| 後台 待建立 | 1 | 1 | 1 |
| 前台 待修改 | 4 | 4 | 4 |
| 前台 待建立 | 11 | 11 | 11 |
| **合計** | **30** | **30** | **30** |

---

*建立日期：2026-06-15*
*更新規則：每完成一項，將對應的 `[ ]` 改為 `[x]`，並在進度總覽手動累計數字*
